package com.writeloop.persistence;

import org.springframework.data.jpa.repository.JpaRepository;

public interface FeedbackDiagnosisLogRepository extends JpaRepository<FeedbackDiagnosisLogEntity, Long> {
}
