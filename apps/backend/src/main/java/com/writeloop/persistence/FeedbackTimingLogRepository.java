package com.writeloop.persistence;

import org.springframework.data.jpa.repository.JpaRepository;

public interface FeedbackTimingLogRepository extends JpaRepository<FeedbackTimingLogEntity, Long> {
}
